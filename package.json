{
  "name": "nature-of-code",
  "version": "1.0.0",
  "description": "My code along of the book nature of code",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "devDependencies": {
    "@types/p5": "^0.9.0",
    "npm-run-all": "^4.1.5",
    "browser-sync": "^2.26.7",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "@types/lodash": "^4.14.149",
    "lodash": "^4.17.15"
  }
}
